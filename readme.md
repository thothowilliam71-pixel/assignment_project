                ASSIGNMENT PROJECT
Hash_table
 as we know the hash table stores data in memory through the process,, hashing
 the programme as from the title takes the registration number which is the key and stores it with a student name which is the value..the process is called key value mapping
 the hash function inputted to decide where to put the information in memory

 Binary_Search. code
 in the programm each node holds a registration number,name,left and right child pointers . the insert will go left if the registration number is smaller,, right if it is largert
 the code follows a traversal order(Left → Root → Right)  therfore printing records in the order of reg number.
 the Search scans both subtrees since  the names aren't ordered only the registration numbers ar ordered

Factoriol_programm
the program has a function that keeps calling itself with smaller values it would loop infinatley but with the help of the base case the loop stops recursion it uses the idea of the factorial formular

Graph_rep code
the code has a way to store the graph through a dictionary where the key is the vertex and the value is the list of connected vertices 
when we add the edges u and v the programm connects the 2 vertices if there is no vertex you need to create it then add the neigbour
